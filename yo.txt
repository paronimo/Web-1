:root {
    --primary: rgb(126, 40, 77);
    --primary-soft: rgba(126, 40, 77, 0.12);
    --text: #222;
    --surface: #faf7f9;
    --surface-strong: #fff;
    --border: rgba(126, 40, 77, 0.18);
}
.register-container {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    min-height: 100vh;
    padding: 40px 20px;
}

.register-container h2 {
    margin-top: 0;
    margin-bottom: 20px;
    color: var(--text);
}

.register {
    display: flex;
    flex-direction: column;
    justify-content: center;
    gap: 12px;
    max-width: 400px;
    width: 100%;
    padding: 22px;
    background: var(--surface-strong);
    border: 1px solid var(--border);
    border-radius: 28px;
    box-shadow: 0 10px 24px rgba(126, 40, 77, 0.08);
}

.register form {
    display: flex;
    flex-direction: column;
    gap: 12px;
}

.register a {
    text-align: center;
    color: var(--primary);
    text-decoration: none;
    font-weight: 600;
    padding: 10px;
    border-radius: 16px;
    transition: background 0.2s ease, color 0.2s ease;
}

.register a:hover {
    background: var(--primary-soft);
    color: var(--primary);
}
* {
    box-sizing: border-box;
}

body {
    font-family: 'Inter', Arial, sans-serif;
    margin: 0;
    padding: 10;
    color: var(--text);
    background: linear-gradient(180deg, #fffafa 0%, var(--surface) 100%);
}

header {
    background: var(--primary);
    color: white;
    padding: 22px 24px;
    border-radius: 0 0 24px 24px;
    box-shadow: 0 12px 34px rgba(126, 40, 77, 0.12);
}

header h1,
header nav {
    margin: 0;
}

nav {
    display: flex;
    justify-content: center;
    gap: 18px;
    margin-top: 14px;
}

nav a {
    color: rgba(255, 255, 255, 0.92);
    text-decoration: none;
    font-weight: 600;
    padding: 8px 14px;
    border-radius: 999px;
    transition: background 0.25s ease, transform 0.25s ease;
}

nav a:hover {
    background: rgba(255, 255, 255, 0.16);
    transform: translateY(-1px);
}

main {
    max-width: 1180px;
    margin: 24px auto;
    padding: 0 18px 32px;
}

.products {
    max-width: 1180px;
    margin: 24px auto;
    padding: 22px;
    background: var(--surface-strong);
    border: 1px solid var(--border);
    border-radius: 28px;
    box-shadow: 0 10px 24px rgba(126, 40, 77, 0.08);
}

.products h2 {
    margin-top: 0;
    margin-bottom: 20px;
    color: var(--text);
}

.products-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
    gap: 20px;
    justify-items: center;
}

.target {
    display: flex;
    flex-direction: column;
    width: 100%;
    max-width: 200px;
    aspect-ratio: 1 / 1;
    overflow: hidden;
    background: var(--surface-strong);
    border: 1px solid var(--border);
    border-radius: 24px;
    box-shadow: 0 10px 20px rgba(126, 40, 77, 0.08);
    transition: transform 0.2s ease, box-shadow 0.2s ease;
}

.target:hover {
    transform: translateY(-4px);
    box-shadow: 0 18px 42px rgba(126, 40, 77, 0.12);
}

.target a {
    width: 100%;
    height: 100%;
    display: flex;
    flex-direction: column;
    text-decoration: none;
    color: inherit;
}

.target img {
    width: 100%;
    height: 70%;
    object-fit: cover;
    display: block;
}

.target h3,
.target p {
    margin: 0;
    padding: 0;
}

.target-info {
    height: 30%;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    padding: 8px;
    background: white;
}

.target-info h3 {
    font-size: 14px;
    font-weight: 600;
    text-align: center;
}

.target-info p {
    font-size: 12px;
    color: var(--primary);
    font-weight: 700;
    text-align: center;
}

.card {
    background: var(--surface-strong);
    border: 1px solid var(--border);
    border-radius: 22px;
    padding: 22px;
    margin: 0;
    box-shadow: 0 12px 30px rgba(126, 40, 77, 0.08);
    transition: transform 0.2s ease, box-shadow 0.2s ease;
}

.card:hover {
    transform: translateY(-4px);
    box-shadow: 0 18px 42px rgba(126, 40, 77, 0.12);
}

button,
input,
select {
    border-radius: 16px;
    border: 1px solid var(--border);
    font-family: 'Inter', Arial, sans-serif;
    font-size: 14px;
}

input,
select {
    padding: 12px 14px;
    background: var(--surface);
    color: var(--text);
    transition: border-color 0.2s ease, background 0.2s ease;
}

input:focus,
select:focus {
    outline: none;
    border-color: var(--primary);
    background: var(--surface-strong);
}

input::placeholder {
    color: rgba(34, 34, 34, 0.5);
}

button {
    background: var(--primary);
    color: white;
    border: none;
    padding: 12px 18px;
    cursor: pointer;
    font-weight: 700;
    transition: opacity 0.2s ease, transform 0.2s ease;
    width: 100%;
}

button:hover {
    opacity: 0.95;
    transform: translateY(-1px);
}

section,
footer,
article {
    border-radius: 24px;
    background: var(--surface-strong);
    border: 1px solid var(--border);
    padding: 22px;
    margin-top: 20px;
}

footer {
    max-width: 1180px;
    margin: 0 auto 24px;
    background: transparent;
    border: 0;
    color: #6d5e6f;
    text-align: center;
}

y aqui el index.ejs que ya sabes donde se encuentra... en cuanto al resto lo tengo normal e igual
<%- include('../partials/header') %>
<div class="products">
    <h2>Productos</h2>
    <div class="products-grid">
        <div class="target">
            <a href="/products">
                <img src="../images/macbookNEO.png">
                <div class="target-info">
                    <h3>Macbook NEO</h3>
                    <p>100 pts</p>
                </div>
            </a>
        </div>
        <div class="target">
            <a href="/products">
                <img src="../images/botellita.png">
                <div class="target-info">
                    <h3>Botellita</h3>
                    <p>170 pts</p>
                </div>
            </a>
        </div>
        <div class="target">
            <a href="/products">
                <img src="../images/botella-verde.png">
                <div class="target-info">
                    <h3>Botella Verde</h3>
                    <p>165 pts</p>
                </div>
            </a>
        </div>
    </div>
</div>
<%- include('../partials/footer') %>

